import { Container } from '../../styles'
import { HeroContainer, FormHero, HeroTitle } from './styles'

const Hero = () => (
  <FormHero>
    <Container>
      <HeroContainer>
        <HeroTitle>
          As melhores vagas para tecnologia, design e artes visuais.
        </HeroTitle>
      </HeroContainer>
    </Container>
  </FormHero>
)

export default Hero
